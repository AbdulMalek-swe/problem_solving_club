#include <bits/stdc++.h>
using namespace std;
#define ll long long int
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    ll a,b;
    cin >> a >> b;
    ll sum = 0;
    sum = (a*5000) + (b*9800);
    cout << sum<<endl;
    return 0;
}